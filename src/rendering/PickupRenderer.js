
export class PickupRenderer {
    constructor(scene, _maxPickups = 100) {
        this.scene = scene;
        // No pickups to render
    }

    setGlowIntensity(_intensity) {
        // No-op
    }

    render(_pickups, _interpolation) {
        // No-op
    }
}
